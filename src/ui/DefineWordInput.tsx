import * as React from "react";
import styled from "styled-components";
import { MyInputbox } from "../styles/Inputbox";

export const DefineWordInput = () => {
  return <MyInputbox placeholder="Urban dictionary lookup" />;
};
